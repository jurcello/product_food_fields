<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="food_product_view_form" model="ir.ui.view">
        <field name="name">food_product_fields.extra.fields</field>
        <field name="model">product.template</field>
        <field name="priority" eval="9" />
        <field name="inherit_id" ref="product.product_template_only_form_view" />
        <field name="arch" type="xml">
            <xpath expr="//page[@name='inventory']" position="after">
                <page string="Food related" name="food">
                    <group name="group_default">
                        <field name="ingredients" string="Ingrediënten" />
                    </group>
                </page>
            </xpath>
            <field name="barcode" position="after">
                <field name="plu_code" />
            </field>
        </field>

    </record>
</odoo>
